<script setup>
    import Basic from './Basic.vue'
    import EmbellishedNavbar from './EmbellishedNavbar.vue'
    import HmburgerNavbar from './HmburgerNavbar.vue'
    import DropdownNavbar from './DropdownNavbar.vue'
    import DarkModeNavbar from './DarkModeNavbar.vue'
    import ScrollEfectNavbar from './ScrollEfectNavbar.vue'
    import RightSidebar from './RightSidebar.vue'
    import ExpandedSidebar from './ExpandedSidebar.vue'
</script>

<template>
    <p>Basic navbar (PrimeVue)</p>
    <Basic />
    <br></br>
    <p>Embellished basic navbar (PrimeVue)</p>
    <EmbellishedNavbar />
    <p> Hamburger navbar (PrimeVue)</p>
    <HmburgerNavbar />
    <p> Dropdown navbar (PrimeVue)</p>
    <DropdownNavbar />
    <p> Dark mode navbar (PrimeVue)</p>
    <DarkModeNavbar />
    <p> Scroll effect navbar (PrimeVue)</p>
    <p>At top of page (it scrolls with page scrolling)</p>
    <ScrollEfectNavbar />
    <p> Right sidebar (PrimeVue)</p>
    <RightSidebar />
    <p> Expanded sidebar - on the left hand side (PrimeVue)</p>
    <ExpandedSidebar /> 
</template>

